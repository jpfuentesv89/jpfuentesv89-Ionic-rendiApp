import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.web.rendiApp',
  appName: 'rendiApps',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
